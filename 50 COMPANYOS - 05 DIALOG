/*************************************************
 * 50 COMPANYOS - 05 DIALOG
 * Version: 1.0.0
 *************************************************/

function openCompanyOS() {
  companyOSEnsureDatabase_();
  const html = HtmlService.createTemplateFromFile("CompanyOSDialog")
    .evaluate().setWidth(1180).setHeight(760);
  SpreadsheetApp.getUi().showModalDialog(html, "CompanyOS — Company Roster");
}

function launchCompanyOS() { openCompanyOS(); }
